using UnityEngine;
using System.Collections;

public class TowerGenerator : MonoBehaviour {

  void Start () {
    // var towerResource = Resources.Load("Tower");
    // int i = 0;
    // while (i < 5) {
    //   Quaternion spawnRotation = Quaternion.identity;
    //   var tower = (GameObject)Instantiate(towerResource, new Vector3(5*i,0,1), new Quaternion());
    //   tower.transform.position = Vector3.MoveTowards(tower.transform.position, new Vector3(0, 0, 0) + transform.position, 1.0f);
    //   ++i;
    // }
  }

  // Update is called once per frame
  void Update () {
  }
}
